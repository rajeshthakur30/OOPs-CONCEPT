# Restore GitHub Commit Setup

## Steps:
- [x] Recreate .gitignore
- [x] Recreate compile.bat in Lect2ConstructorandDestructor/
- [ ] git add .
- [ ] git commit -m "Restore .gitignore and compile.bat for proper GitHub commits"
- [x] git push origin blackboxai/fix-exe-build
- [x] Check/create PR with gh pr (run `gh auth login` first)
